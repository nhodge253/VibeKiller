'use strict';

/**
 * @ngdoc function
 * @name vibeKillerApp.controller:FestiveCtrl
 * @description
 * # FestiveCtrl
 * Controller of the vibeKillerApp
 */
angular.module('vibeKillerApp')
  .controller('FestiveCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
